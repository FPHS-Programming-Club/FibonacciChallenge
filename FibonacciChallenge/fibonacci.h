#pragma once

#include "number.h"
using number = big::number;

// You can add stuff here if you want (why would you need to?),
// but removing things will break the timing script.

void test_start();
number calculate(int num);
