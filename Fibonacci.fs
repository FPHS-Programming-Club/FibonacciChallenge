module Fibonacci

open System.Numerics

// You edit this file. Both methods need to exist and be valid,
// but other than that you have complete control of this file.
//
// All methods are included in the timer.

// F#-specific notice: You can mark either function as recursive
//                     if need be.


// Invoked when the test first starts.
let testStart() = 
    ignore()

// Invoked sequentially.
let calculate(num: int): BigInteger =
    BigInteger(0)
